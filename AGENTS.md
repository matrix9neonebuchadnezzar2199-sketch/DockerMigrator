# Agent notes (DockerMigrator)

## Git / リモート

- **Canonical remote**: `https://github.com/matrix9neonebuchadnezzar2199-sketch/DockerMigrator`（`origin` がこれであること）。
- **コードや設定を修正したセッションでは、作業完了時に必ず `git push` まで行う**（ローカルだけで終えない）。まとまった単位でコミットし、`main`（または作業ブランチ）を `origin` に反映する。

## README（ルート `README.md`）

- 利用者・コントリビュータ向けの **公式エントリ** とみなす。
- **機能・要件・バージョン・タグ／Releases の方針・リポジトリ構成** などに変更があったら、**同一 PR／コミットで README を更新するか、続けのコミットで必ず追随** させる（README だけ古い状態を残さない）。
