# DockerMigrator v0.3.0-poc — Phase 7 & UI 第4回・M1–M5 完了

**Release date**: 2026-05-19  
**Git tag**: `v0.3.0-poc`  
**App version**: `dmig/package.json` → `0.3.0-poc`

---

## ハイライト

### Phase 7 — テスト厚め化

- `importImages` 新シグネチャ、ComposeImporter 経路、Exporter 中断・再開、`dmig:resumeExport` IPC、`validatePartialState` 境界、`DockerAdapter` の `DOCKER_HOST` 分岐などを追加（テスト 19 → 44 件規模、現行スイートは **101 passed**）。

### Phase 6 第4回 — UI 再設計

| Step | 内容 |
|------|------|
| A | サイドバー 3 グループ（移行元 / 移行先 / 共通）、`ResumePage`、`dmig:listResumablePackages` |
| F | ヘルプ / 用語集（18 語・`#hash` 深リンク） |
| C | `SourceOverview` / `TargetOverview`、起動時 `source-overview` |
| E | `NextStepFooter`（次にやること・静的） |
| D | `StepIndicator`（現在地・移行先 2 段） |
| B | **撤回** — 初回ウィザード（UX 二重化のため） |

三層 UX: **Overview = 地図**、**Help = 辞書**、**Footer + Indicator = コンパス / 現在地**。

### M1–M5 — PoC 仕上げ

| M | 内容 |
|---|------|
| M1 | 手順書アーカイブ、`0.3.0-poc` 版上げ |
| M2 | Sidebar `lucide-react`、用語集→概要導線 |
| M3 | 設定最小（`restoreLastPage` / `defaultExportDir` / `lastPage`） |
| M4 | Docker 未接続 Footer、`useResumeFlow`、Step クリック遷移 |
| M5 | `smoke-checklist.md`、`run_smoke_check.py`、`App.integration.test.tsx` |

---

## テスト

- **101 passed** / **1 skipped**（Windows symlink 関連 1 件）
- 自動 smoke: `python scripts/run_smoke_check.py`（`typecheck` / `lint` / `test` / `build` + Docker 確認）

---

## 既知の制限

- **バイナリ未添付** — インストーラ正式化は M13 予定。
- **WSL2 丸ごと** — 未対応（M11）。
- **ドライラン UI（A）/ ロールバック（I）** — 仕様書 Phase 6 行相当は M9 / M10 で着手予定。
- Electron DevTools の Autofill 警告は既知ノイズ（動作影響なし）。

---

## 次マイルストーン（確定順）

**M6**（本リリース整備）→ **M8**（Footer 動的 CTA・ログビューア）→ **M9** → **M10** → **M7**（設定束ね）→ **M11** → **M12** → **M13**

詳細: [M6–M13 ロードマップ](../milestones/M6-M13-roadmap.md)

---

## 関連ドキュメント

- [CHANGELOG.md](../../CHANGELOG.md)
- [仕様書.txt](../../仕様書.txt) §9.1（Phase↔M 対応表）
- [UI 再設計メモ](../dmig-ui-redesign-v0.1.md)
- [手動スモークチェックリスト](../testing/smoke-checklist.md)
